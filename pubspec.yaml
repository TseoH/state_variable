name: state_variable
description: An advanced variable useful to define your different state for State Management.
version: 0.0.1+1
publish_to: none

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  equatable: ^2.0.3

dev_dependencies:
  mocktail: ^0.3.0
  test: ^1.21.4
  very_good_analysis: ^3.0.1
